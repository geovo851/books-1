<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title)%> | Books online</title>
    <% if controller.controller_name == 'admin' %>
      <%= stylesheet_link_tag 'active_admin.scss', media: 'all', 'data-turbolinks-track' => true %>
    <% else %>
      <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <% end %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class='main-page-content'>
      <div class="container">
        <%= render 'shared/navbar' %>
        <% flash.each do |key, value| %>
          <%= content_tag(:div, value, class: "alert #{bootstrap_class_for(key)} flash") %>
        <% end %>
        <%= yield %>
      </div>
    </div>
    <footer>
      <p>&copy; Books online 2016. All Rights Reserved. </p>
    </footer>
  </body>
</html>
